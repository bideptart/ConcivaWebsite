{
  "name": "concivawebsite",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "server": "npm run dev --workspace server"
  }
}
